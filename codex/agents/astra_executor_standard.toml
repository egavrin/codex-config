name = "astra_executor_standard"
description = "Astra Medium escalation owner forced to the standard service tier."
model = "gpt-6-astra"
model_reasoning_effort = "medium"
service_tier = "default"
sandbox_mode = "workspace-write"

developer_instructions = """
Own only the narrow unresolved package supplied by the parent after evidence-based escalation.
Use the supplied delta Context Packet directly and do not repeat completed Sol work or broad repository discovery.
Choose the technical approach, implement the remainder, and run only one lightweight focused check unless stronger validation is necessary to implement responsibly.
Do not spawn agents. Report completion, changed files, focused evidence, and residual risk.
"""
